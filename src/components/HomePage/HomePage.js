import React, { PropTypes } from 'react';
import './HomePage.scss';

class ErrorPage {

    render() {
        let title = 'Home';

        return (
            <div>
                <h1>{title}</h1>
            </div>
        );
    }

}

export default ErrorPage;
